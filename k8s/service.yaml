apiVersion: v1
kind: Service
metadata:
  name: graphmind-api
  namespace: graphmind
  labels:
    app.kubernetes.io/name: graphmind
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: graphmind
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: graphmind
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
